use_gpu: True
device: 5  # GPU 5 reserved for VPL-GP algorithm
federate:
  save_to: "/hdd/hdd3/kjb/checkpoints/hhrl_choice_gemma_fedbiscuit_u3_vplgp_50100.ckpt"
model:
  type: 'google/gemma-2b@huggingface_llm'
llm:
  use: True
  # VPL-GP parameters (must match the selector checkpoint)
  vpl_latent_dim: 32
  vpl_feature_method: 'choice_logits'
  vpl_gp_temperature: 1.0
