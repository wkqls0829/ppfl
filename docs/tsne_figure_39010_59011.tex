\begin{figure*}[t]
    \centering
    \includegraphics[width=\textwidth]{figures/cross_client_z_tsne_39010_59011_2x4_paper.png}
    \caption{
        Evolution of client-specific latent preference distributions ($z$) across training rounds for \textbf{FedVPL} (top row) and \textbf{FedVPA-GP} (bottom row).
        Points are colored by preference type: red (harmlessness) and blue (helpfulness).
        Star markers ($*$) indicate orthogonal prototypes in FedVPA-GP.
        FedVPA-GP achieves better separation between preference types compared to FedVPL.
    }
    \label{fig:z_distribution_evolution}
\end{figure*}
